const borderWidth = {
  // Assuming getMeasureProperty is a function that returns the desired property value
  borderWidth: getMeasureProperty(context, { propertyKey: "borderWidth" }).toUpperCase()
};

export default borderWidth;
